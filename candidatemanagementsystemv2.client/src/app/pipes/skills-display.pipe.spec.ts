import { SkillsDisplayPipe } from './skills-display.pipe';

describe('SkillsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
