import { PositionsDisplayPipe } from './positions-display.pipe';

describe('PositionsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
